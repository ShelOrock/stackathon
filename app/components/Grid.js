import React from 'react';

const Grid = () => {
    return (
        <div>
            {/* {
               new Array(Math.floor(window.innerWidth / 4)).fill('').map(item => {
                   return (
                       <div style={{ width: '25px', height: '25px', border: '1px black solid'}}></div>
                   )
               })
            } */}
        </div>
    )
}

export default Grid;