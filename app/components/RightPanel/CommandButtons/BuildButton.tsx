import React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledButton: { LinkButton } } = StyledComponents;

export default () => <LinkButton to='/3D'>Build it</LinkButton>;