import React from "react";
import { useAppDispatch } from "../../hooks";

const Roof = ({
  id,
  label,
  isDisabled,
  isHidden,
  isHighlighted,
  isLocked,
  height,
  width,
  orientation,
  xPosition,
  yPosition,
  tag
}) => {
  const GRID_SNAP = 25;

  const dispatch = useAppDispatch();

  return <></>
};

export default Roof;
