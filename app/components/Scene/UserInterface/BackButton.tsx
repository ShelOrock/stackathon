import React from 'react';

import * as StyledComponents from '../../StyledComponents';
const {
  StyledButton: { LinkButton },
} = StyledComponents;

export default () => <LinkButton to='/'>BACK</LinkButton>;